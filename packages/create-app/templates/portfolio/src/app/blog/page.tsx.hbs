import { Badge, Card, CardHeader, CardTitle, CardContent } from '@ccl/ui';

export default function BlogPage() {
  return (
    <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
      <div className="text-center mb-12">
        <Badge className="mb-4">Blog</Badge>
        <h1 className="text-4xl font-bold mb-4">Writing & Insights</h1>
        <p className="text-lg text-gray-600 max-w-2xl mx-auto">
          Thoughts on engineering leadership, scalable architecture, and developer experience.
        </p>
      </div>
      <div className="grid gap-6 md:grid-cols-2">
        {[1,2,3].map(i => (
          <Card key={i}>
            <CardHeader>
              <CardTitle>Draft Post #{i}</CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-sm text-gray-600">MDX content integration coming soon. This is a placeholder post entry generated by the template.</p>
            </CardContent>
          </Card>
        ))}
      </div>
    </div>
  );
}
